rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 개발 편의를 위해 일시적으로 모든 사용자 허용
      // 추후 보안을 위해 'if request.auth != null'로 변경 권장
      allow read, write: if true;
    }
  }
}
